!!!
%html
%body
  %p This data comes from #{@pdx_count} missed connection posts from Portland, OR and #{@nyc_count} posts from New York City.
 
%img{:src=> "#{pdx_who4who_graph}"}

%img{:src=> "#{nyc_who4who_graph}"}

%h2 The most common nouns are:

%div{:class=> "nouns"}
  %h2 PDX 
  %ul
  - @pdx_nouns.each do |noun, value|
    %li= noun

%div{:class=> "nouns"}
  %h2 NYC
  - @nyc_nouns.each do |noun, value|
    %li= noun

%p #{@common_adjectives}

%img{:src=> "#{pdx_day_of_week_graph}"}

%img{:src=> "#{nyc_day_of_week_graph}"}

%h2 A generic missed connection post from PDX might look like this: 

%p #{@generic_pdx}

%h2 A generic missed connection post from NYC might look like this:

%p #{@generic_nyc}

